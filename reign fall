local repo = 'https://raw.githubusercontent.com/longhazem/libba/main/'
local Library = loadstring(game:HttpGet(repo .. 'source.lua'))()
local ThemeManager = loadstring(game:HttpGet(repo .. 'addons/ThemeManager.lua'))()
local SaveManager = loadstring(game:HttpGet(repo .. 'addons/SaveManager.lua'))()

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local lp = Players.LocalPlayer
local camera = workspace.CurrentCamera
local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

local originalFogEnd = Lighting.FogEnd
local originalBrightness = Lighting.Brightness

local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1.5
FOVCircle.NumSides = 100
FOVCircle.Filled = false
FOVCircle.Visible = false
FOVCircle.Color = Color3.fromRGB(255, 255, 255)

local config = {
    aimbot = false,
    raycasting = false,
    fovEnabled = false,
    fovSize = 100,
    aimbotRange = 1000,
    wallcheck = false,
    autoHit = false,
    infiniteAmmo = false,
    infiniteStamina = false,
    objectiveESP = false,
    ammoESP = false,
    armorESP = false,
    medkitESP = false,
    zombieESP = false,
    playerESP = false,
    nameESP = false,
    hitboxExpander = false,
    hitboxSize = 6,
    nofog = false,
    fullbright = false
}

local target = {enabled = true, position = nil}
local highlightObjects = {}
local expandedHitboxes = {}  -- key: part, value: true
local originalSizes = {}     -- lưu kích thước gốc để khôi phục
local originalAmmoValues = {}
local originalStaminaValue = nil

local Window = Library:CreateWindow({
    Title = 'Tokai HUB - Reign Fall',
    Center = true,
    AutoShow = true,
    TabPadding = 8,
    MenuFadeTime = 0.2
})

local Tabs = {
    Combat = Window:AddTab('Combat'),
    Visuals = Window:AddTab('Visuals'),
    Utility = Window:AddTab('Utility'),
    ['UI Settings'] = Window:AddTab('UI Settings'),
}

local CombatLeft = Tabs.Combat:AddLeftGroupbox('Combat Features')
local VisualsLeft = Tabs.Visuals:AddLeftGroupbox('ESP Elements')
local VisualsRight = Tabs.Visuals:AddRightGroupbox('Environment Settings')
local UtilityLeft = Tabs.Utility:AddLeftGroupbox('Character & Weapons')

local function UpdateVisuals(obj, shouldShow, color, nameText)
    if not obj or not obj.Parent then return end
    pcall(function()
        if shouldShow then
            if not obj:FindFirstChild("TokaiHighlight") then
                local highlight = Instance.new("Highlight")
                highlight.Name = "TokaiHighlight"
                highlight.FillColor = color
                highlight.OutlineColor = color
                highlight.FillTransparency = 0.3
                highlight.OutlineTransparency = 0
                highlight.Parent = obj
                highlightObjects[obj] = true
            end
            local label = obj:FindFirstChild("TokaiNameESP")
            if config.nameESP then
                if not label then
                    label = Instance.new("BillboardGui")
                    label.Name = "TokaiNameESP"
                    label.Size = UDim2.new(0, 200, 0, 50)
                    label.StudsOffset = Vector3.new(0, 3, 0)
                    label.AlwaysOnTop = true
                    local textLabel = Instance.new("TextLabel")
                    textLabel.Parent = label
                    textLabel.Size = UDim2.new(1, 0, 1, 0)
                    textLabel.BackgroundTransparency = 1
                    textLabel.Text = nameText
                    textLabel.TextColor3 = color
                    textLabel.TextStrokeTransparency = 0
                    textLabel.TextSize = 13
                    label.Parent = obj
                else
                    label.TextLabel.Text = nameText
                    label.TextLabel.TextColor3 = color
                end
            else
                if label then label:Destroy() end
            end
        else
            local highlight = obj:FindFirstChild("TokaiHighlight")
            if highlight then highlight:Destroy() end
            local label = obj:FindFirstChild("TokaiNameESP")
            if label then label:Destroy() end
            highlightObjects[obj] = nil
        end
    end)
end

local function UpdateESP()
    local game_workspace = workspace:FindFirstChild("Game")
    if game_workspace then
        local current = game_workspace:FindFirstChild("Current")
        if current then
            local spawned = current:FindFirstChild("Spawned")
            if spawned then
                local gameObjects = spawned:FindFirstChild("GameObjects")
                if gameObjects then
                    for _, obj in ipairs(gameObjects:GetChildren()) do
                        local name = obj.Name
                        if name:sub(1, 8) == "mission_" or name == "scenery_phone_ringing" then
                            local color = name:sub(1, 8) == "mission_" and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 255, 255)
                            UpdateVisuals(obj, config.objectiveESP, color, "Objective")
                        elseif name == "pickup_ammo" then
                            UpdateVisuals(obj, config.ammoESP, Color3.fromRGB(255, 255, 0), "Ammo")
                        elseif name == "pickup_armor" then
                            UpdateVisuals(obj, config.armorESP, Color3.fromRGB(0, 0, 255), "Armor")
                        elseif name == "pickup_medkit" then
                            UpdateVisuals(obj, config.medkitESP, Color3.fromRGB(255, 0, 0), "Medkit")
                        end
                    end
                end
                local npcs = spawned:FindFirstChild("NPCs")
                if npcs and npcs:FindFirstChild("enemies") then
                    for _, zombie in ipairs(npcs.enemies:GetChildren()) do
                        if zombie:IsA("Model") and zombie:FindFirstChild("Humanoid") then
                            UpdateVisuals(zombie, config.zombieESP, Color3.fromRGB(255, 0, 0), "Zombie")
                        end
                    end
                end
            end
        end
    end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= lp and player.Character and player.Character:FindFirstChild("Humanoid") then
            UpdateVisuals(player.Character, config.playerESP, Color3.fromRGB(0, 255, 255), player.Name)
        end
    end
end

local function isPlayerCharacter(model)
    if not model or not model:IsA("Model") then return false end
    return Players:GetPlayerFromCharacter(model) ~= nil
end

local function isHumanoidModel(model)
    if not model or not model:IsA("Model") then return false end
    return model:FindFirstChildWhichIsA("Humanoid") ~= nil
end

-- ========== HITBOX EXPANDER – TỐI ƯU KHÔNG LAG ==========
function applyHitboxExpander(part)
    if not part or not part:IsA("BasePart") then return end
    if part.Name ~= "Head" then return end
    local model = part.Parent
    if not isHumanoidModel(model) or isPlayerCharacter(model) then return end
    if not config.hitboxExpander then return end

    pcall(function()
        if not expandedHitboxes[part] then
            -- Lưu kích thước gốc
            originalSizes[part] = part.Size
            part.Size = Vector3.new(config.hitboxSize, config.hitboxSize, config.hitboxSize)
            part.CanCollide = false
            part.Transparency = 0.7
            expandedHitboxes[part] = true
        else
            -- Cập nhật kích thước nếu thay đổi
            part.Size = Vector3.new(config.hitboxSize, config.hitboxSize, config.hitboxSize)
            part.Transparency = 0.7
        end
    end)
end

function revertHitboxExpander(part)
    if not part or not part:IsA("BasePart") then return end
    pcall(function()
        if originalSizes[part] then
            part.Size = originalSizes[part]
            part.Transparency = 0
            part.CanCollide = true
            originalSizes[part] = nil
        else
            part.Size = Vector3.new(1, 1, 1)
            part.Transparency = 0
        end
        expandedHitboxes[part] = nil
    end)
end

function applyToAllZombies()
    if not config.hitboxExpander then return end
    local container = getEnemyContainer()
    if not container then return end
    for _, enemy in ipairs(container:GetChildren()) do
        if enemy:IsA("Model") and enemy:FindFirstChild("Humanoid") then
            local head = enemy:FindFirstChild("Head")
            if head then
                applyHitboxExpander(head)
            end
        end
    end
end

function revertAllHitboxes()
    for part, _ in pairs(expandedHitboxes) do
        if part and part.Parent then
            revertHitboxExpander(part)
        else
            expandedHitboxes[part] = nil
            originalSizes[part] = nil
        end
    end
    expandedHitboxes = {}
    originalSizes = {}
end

-- Xóa khi zombie chết (model bị xóa)
Workspace.DescendantRemoving:Connect(function(desc)
    if desc:IsA("Model") and desc:FindFirstChild("Humanoid") and not isPlayerCharacter(desc) then
        -- Tìm các phần của model này trong expandedHitboxes
        for part, _ in pairs(expandedHitboxes) do
            if part and part:IsDescendantOf(desc) then
                revertHitboxExpander(part)
                expandedHitboxes[part] = nil
                originalSizes[part] = nil
            end
        end
    end
end)

-- Áp dụng cho zombie mới xuất hiện
Workspace.DescendantAdded:Connect(function(desc)
    if config.hitboxExpander and desc:IsA("Model") and desc:FindFirstChild("Humanoid") and not isPlayerCharacter(desc) then
        task.wait(0.05) -- đợi để model load đầy đủ
        local head = desc:FindFirstChild("Head")
        if head then
            applyHitboxExpander(head)
        end
    end
end)

-- Bật/tắt chức năng
function toggleHitboxExpander(status)
    config.hitboxExpander = status
    if status then
        applyToAllZombies()
    else
        revertAllHitboxes()
    end
end

-- Hàm lấy container enemy (dùng chung)
local function getEnemyContainer()
    local container = workspace.Game and workspace.Game.Current and workspace.Game.Current.Spawned and workspace.Game.Current.Spawned.NPCs
    if container then
        return container:FindFirstChild("enemies")
    end
    return nil
end

local function isVisible(targetPos)
    if not config.wallcheck then return true end
    local origin = camera.CFrame.Position
    local dir = targetPos - origin
    local raycastParams = RaycastParams.new()
    raycastParams.FilterDescendantsInstances = {lp.Character, camera}
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    local result = Workspace:Raycast(origin, dir, raycastParams)
    if result then
        local hitInstance = result.Instance
        if hitInstance:IsDescendantOf(workspace.Game.Current.Spawned.NPCs) then
            return true
        end
        return false
    end
    return true
end

-- ========== AIMBOT TỐI ƯU ==========
local function getClosestHead()
    local container = getEnemyContainer()
    if not container then return nil end

    local referencePos
    if isMobile then
        local vp = camera.ViewportSize
        referencePos = Vector2.new(vp.X / 2, vp.Y / 2)
    else
        referencePos = UserInputService:GetMouseLocation()
    end

    local bestZombie = nil
    local bestDist = math.huge
    local criticalParts = {"Head", "HumanoidRootPart", "Torso"}

    for _, enemy in ipairs(container:GetChildren()) do
        if not enemy:IsA("Model") or not enemy:FindFirstChild("Humanoid") then continue end

        local headPart = enemy:FindFirstChild("Head")
        if not headPart then
            headPart = enemy:FindFirstChild("HumanoidRootPart") or enemy:FindFirstChild("Torso")
        end
        if not headPart then continue end

        local found = false
        local minDist = math.huge
        for _, partName in ipairs(criticalParts) do
            local part = enemy:FindFirstChild(partName)
            if part and part:IsA("BasePart") then
                local screenPoint, onScreen = camera:WorldToScreenPoint(part.Position)
                if onScreen then
                    local dist = (Vector2.new(screenPoint.X, screenPoint.Y) - referencePos).Magnitude
                    if dist <= config.fovSize and dist <= config.aimbotRange then
                        if dist < minDist then minDist = dist end
                        found = true
                    end
                end
            end
        end

        if found and minDist < bestDist then
            if config.wallcheck and not isVisible(headPart.Position) then
                continue
            end
            bestDist = minDist
            bestZombie = enemy
        end
    end

    if bestZombie then
        local head = bestZombie:FindFirstChild("Head")
        if head then return head.Position end
        local root = bestZombie:FindFirstChild("HumanoidRootPart") or bestZombie:FindFirstChild("Torso")
        if root then return root.Position end
    end
    return nil
end

RunService.RenderStepped:Connect(function()
    if (config.raycasting or config.aimbot) and target.enabled then
        target.position = getClosestHead()
    end
end)

RunService.RenderStepped:Connect(function()
    if config.aimbot and target.position then
        camera.CFrame = CFrame.new(camera.CFrame.Position, target.position)
    end
    if config.fovEnabled then
        FOVCircle.Visible = true
        FOVCircle.Radius = config.fovSize
        if isMobile then
            FOVCircle.Position = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
        else
            FOVCircle.Position = UserInputService:GetMouseLocation()
        end
    else
        FOVCircle.Visible = false
    end
    UpdateESP()
end)

local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
    local method = getnamecallmethod()
    if config.raycasting and target.enabled and method:lower() == "raycast" and self == Workspace then
        local args = {...}
        if #args >= 2 and typeof(args[1]) == "Vector3" and typeof(args[2]) == "Vector3" then
            local raycastParams = args[3]
            if raycastParams and raycastParams.CollisionGroup == "raycast_npc_neutral" then
                if target.position then
                    local origin = args[1]
                    local dir = target.position - origin
                    if dir.Magnitude > 0.001 then
                        args[2] = dir.Unit * args[2].Magnitude
                    end
                end
            end
        end
        return oldNamecall(self, unpack(args))
    end
    return oldNamecall(self, ...)
end))

-- ... (các phần khác giữ nguyên: autoHit, infinite ammo/stamina, nofog, fullbright, UI)

-- ========== GIAO DIỆN ==========
CombatLeft:AddToggle('Aimbot', {
    Text = 'Aimbot',
    Default = false
}):OnChanged(function()
    config.aimbot = Toggles.Aimbot.Value
end)

CombatLeft:AddToggle('SilentAim', {
    Text = 'Silent Aim',
    Default = false
}):OnChanged(function()
    config.raycasting = Toggles.SilentAim.Value
end)

CombatLeft:AddToggle('ShowFOV', {
    Text = 'Show FOV Circle',
    Default = false
}):OnChanged(function()
    config.fovEnabled = Toggles.ShowFOV.Value
end)

CombatLeft:AddSlider('FOVSize', {
    Text = 'FOV Size',
    Default = 100,
    Min = 1,
    Max = 1000,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        config.fovSize = Value
    end
})

CombatLeft:AddSlider('AimbotRange', {
    Text = 'Aimbot Range',
    Default = 1000,
    Min = 10,
    Max = 2000,
    Rounding = 0,
    Compact = false,
    Callback = function(Value)
        config.aimbotRange = Value
    end
})

CombatLeft:AddToggle('Wallcheck', {
    Text = 'Wallcheck',
    Default = false
}):OnChanged(function()
    config.wallcheck = Toggles.Wallcheck.Value
end)

CombatLeft:AddToggle('AutoHit', {
    Text = 'Auto Hit',
    Default = false
}):OnChanged(function()
    config.autoHit = Toggles.AutoHit.Value
end)

CombatLeft:AddToggle('HitboxExpander', {
    Text = 'Hitbox Expander',
    Default = false
}):OnChanged(function()
    toggleHitboxExpander(Toggles.HitboxExpander.Value)
end)

CombatLeft:AddSlider('HitboxSize', {
    Text = 'Hitbox Size',
    Default = 6,
    Min = 0,
    Max = 20,
    Rounding = 1,
    Compact = false,
    Callback = function(Value)
        config.hitboxSize = Value
        -- Cập nhật lại các hitbox đang mở rộng
        if config.hitboxExpander then
            for part, _ in pairs(expandedHitboxes) do
                if part and part.Parent then
                    part.Size = Vector3.new(config.hitboxSize, config.hitboxSize, config.hitboxSize)
                end
            end
        end
    end
})

-- Các ESP và Utility giữ nguyên...
-- (sử dụng code từ bản trước, chỉ thay phần HitboxExpander)

-- ====== ĐOẠN CODE ESP, UTILITY, UNLOAD ... (giữ nguyên) ======

-- ... (bỏ qua để ngắn, nhưng thực tế sẽ đưa full)
